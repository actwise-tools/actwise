# ActWise ActOne-Ops MCP — runtime deps (Streamable HTTP transport).
# The actone package itself is stdlib + PyYAML only; the server adds the
# FastMCP + uvicorn/starlette stack (mirrors docenter_mcp for easy porting).
mcp>=1.2
uvicorn>=0.27
starlette>=0.37
PyYAML>=6.0
